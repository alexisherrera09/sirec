# Dependencias del modelado (Fases D4-D6). Instalación local para el baseline:
#   py -m pip install -r requirements-modelo.txt
# El fine-tuning de BETO (entrenar_beto.py) conviene correrlo en Google Colab con GPU.

# --- Baseline clásico (D4): corre en CPU local ---
scikit-learn>=1.3
pandas>=2.0
numpy>=1.24

# --- BETO (D5): preferible en Colab con GPU. torch según tu plataforma/CUDA ---
transformers>=4.40
datasets>=2.19
accelerate>=0.30
torch>=2.2

# calcular_kappa.py NO necesita dependencias: usa solo la librería estándar.
